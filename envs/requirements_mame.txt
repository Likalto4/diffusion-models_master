# on top of a python 3.10 conda env

# xformers installs its own version of pytorch, so I suggest installing xformers first
xformers
torchvision

# other pip installations availabe in conda forge but dependable on torch
accelerate
transformers
diffusers
datasets
bitsandbytes