name: mame_2
channels:
  - conda-forge
dependencies:
  # base
  - python=3.11
  - ipykernel
  - pip-autoremove
  # cuda-related
  - pytorch
  - xformers`
  - torchvision
  # basic
  - numpy
  - pandas
  - matplotlib
  - pillow
  - wandb
  # Hugging Face
  - accelerate
  - datasets
  - transformers
  - diffusers
  # GUI
  - gradio

# - bitsandbytes # install individually